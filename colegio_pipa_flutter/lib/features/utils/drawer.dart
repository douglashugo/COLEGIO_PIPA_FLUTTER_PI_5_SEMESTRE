import 'package:flutter/material.dart';

class DrawerNavegation extends StatelessWidget {
  const DrawerNavegation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Drawer();
  }
}